#==========================================================================
# Description: Build OpenWrt for Lenovo ECB-PR51
# Custom workflow for RK3568 device
#==========================================================================

name: Build OpenWrt for ECB-PR51

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      openwrt_kernel:
        description: "Select kernel version"
        required: false
        default: "6.1.y"
        type: choice
        options:
          - 5.10.y
          - 5.15.y
          - 6.1.y
          - 6.6.y
      auto_kernel:
        description: "Auto use the latest kernel"
        required: false
        default: true
        type: boolean
      openwrt_ip:
        description: "Set OpenWrt default IP address"
        required: false
        default: "192.168.1.1"
      rootfs_source:
        description: "OpenWrt rootfs source repo"
        required: false
        default: "ophub/amlogic-s9xxx-openwrt"
        type: choice
        options:
          - ophub/amlogic-s9xxx-openwrt
          - cuihao99090587/amlogic-s9xxx-openwrt

env:
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialize the build environment
        id: init
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          docker rmi -f $(docker images -q) 2>/dev/null || true
          [[ -n "${AGENT_TOOLSDIRECTORY}" ]] && sudo rm -rf "${AGENT_TOOLSDIRECTORY}"
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android 2>/dev/null
          sudo swapoff -a
          sudo rm -f /swapfile /mnt/swapfile
          sudo -E apt-get -y update
          sudo -E apt-get -y purge azure-cli ghc* zulu* llvm* firefox google* dotnet* powershell openjdk* mongodb* moby* || true
          sudo -E apt-get -y install $(curl -fsSL https://ophub.org/ubuntu2404-make-openwrt-depends)
          sudo -E systemctl daemon-reload
          sudo -E apt-get -y autoremove --purge
          sudo -E apt-get clean
          sudo sed -i '/NVM_DIR/d;/skel/d' /root/{.bashrc,.profile}
          sudo rm -rf ~/{.cargo,.dotnet,.rustup}
          sudo -E timedatectl set-timezone "${TZ:-Etc/UTC}"
          sudo -E ntpdate ntp.ubuntu.com 0.pool.ntp.org || true
          sudo -E timedatectl set-ntp true
          date -u
          timedatectl status || true
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Create virtual disk for extended storage
        run: |
          mnt_size=$(expr $(df -h /mnt | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 1)
          root_size=$(expr $(df -h / | tail -1 | awk '{print $4}' | sed 's/[[:alpha:]]//g' | sed 's/\..*//') - 4)
          sudo truncate -s "${mnt_size}"G /mnt/mnt.img
          sudo truncate -s "${root_size}"G /root.img
          sudo losetup /dev/loop6 /mnt/mnt.img
          sudo losetup /dev/loop7 /root.img
          sudo pvcreate /dev/loop6
          sudo pvcreate /dev/loop7
          sudo vgcreate github /dev/loop6 /dev/loop7
          sudo lvcreate -n runner -l 100%FREE github
          sudo mkfs.xfs -f -i sparse=0 -b size=4096 /dev/github/runner
          sudo mkdir -p /builder
          sudo mount /dev/github/runner /builder
          sudo chown -R runner:runner /builder
          df -Th
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Download OpenWrt rootfs file
        id: download
        working-directory: /builder
        if: ${{ steps.init.outputs.status == 'success' && !cancelled() }}
        run: |
          # Prepare download directory
          armsr_tarfile_path="opt/download"
          [[ -d "${armsr_tarfile_path}" ]] || mkdir -p ${armsr_tarfile_path}
          sudo rm -rf /opt && sudo ln -sf /builder/opt /opt
          ln -sf /builder/opt ${{ github.workspace }}/opt

          # Download rootfs from specified source
          ROOTFS_URL="https://github.com/${{ inputs.rootfs_source }}/releases/download/OpenWrt_lede_master_save_2026.03/openwrt-armsr-armv8-generic-rootfs.tar.gz"
          
          echo "Downloading rootfs from: ${ROOTFS_URL}"
          curl -fsSL "${ROOTFS_URL}" -o "${armsr_tarfile_path}/openwrt-armsr-armv8-generic-rootfs.tar.gz"
          
          if [[ ! -f "${armsr_tarfile_path}/openwrt-armsr-armv8-generic-rootfs.tar.gz" ]]; then
              echo "Error: Failed to download rootfs."
              exit 1
          fi
          
          ls -la ${armsr_tarfile_path}/
          
          echo "build_tag=OpenWrt_ECB-PR51_$(date +"%Y.%m.%d")" >> ${GITHUB_ENV}
          echo "status=success" >> ${GITHUB_OUTPUT}

      - name: Package OpenWrt Firmware for ECB-PR51
        uses: ophub/flippy-openwrt-actions@main
        if: ${{ steps.download.outputs.status == 'success' && !cancelled() }}
        env:
          OPENWRT_ARMSR: opt/download/*rootfs.tar.gz
          PACKAGE_SOC: "diy"
          KERNEL_REPO_URL: "ophub/kernel"
          KERNEL_VERSION_NAME: ${{ inputs.openwrt_kernel }}
          KERNEL_AUTO_LATEST: ${{ inputs.auto_kernel }}
          OPENWRT_IP: ${{ inputs.openwrt_ip }}
          WHOAMI: "ecb-pr51-builder"

      - name: Upload OpenWrt Firmware to Release
        uses: ncipollo/release-action@main
        if: ${{ env.PACKAGED_STATUS == 'success' && !cancelled() }}
        with:
          tag: ${{ env.build_tag }}
          artifacts: ${{ env.PACKAGED_OUTPUTPATH }}/*
          allowUpdates: true
          removeArtifacts: false
          replacesArtifacts: true
          makeLatest: true
          token: ${{ secrets.GITHUB_TOKEN }}
          body: |
            ### OpenWrt for Lenovo ECB-PR51
            - **Device**: Lenovo ECB-PR51 (RK3568)
            - **RAM**: 8GB DDR4
            - **Storage**: 32GB eMMC + SATA
            - **Network**: 2x Gigabit Ethernet
            - **WiFi/BT**: AP6256
            
            ### Default Settings
            - Default IP: `192.168.1.1`
            - Default username: `root`
            - Default password: `password`
            
            ### Install to EMMC
            - Login to OpenWrt → `System` → `Amlogic Service` → `Install OpenWrt`
            
            ### OpenWrt source code
            - [coolsnowwolf/lede](https://github.com/coolsnowwolf/lede)
